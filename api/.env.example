# Required
OPENAI_API_KEY=sk-...

# Optional — set to use a custom OpenAI-compatible endpoint (e.g. Azure, Ollama, LM Studio, vLLM)
# OPENAI_BASE_URL=https://your-proxy-or-local-endpoint/v1

# Optional — defaults to gpt-4o-mini
# MODEL_NAME=gpt-4o

# Required
DB_NAME=
DB_PORT=
DB_HOST=
DB_USER=
DB_PASSWORD=

